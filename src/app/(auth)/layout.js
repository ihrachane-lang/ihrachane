export const metadata = {
  title: "Authentication | IHRACHANE",
  robots: {
    index: false,
    follow: false,
  },
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
